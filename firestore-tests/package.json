{
  "name": "expensetracker-firestore-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security rules unit tests for firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "firebase --config ../firebase.json --project demo-expensetracker emulators:exec --only firestore \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.5.0"
  }
}
